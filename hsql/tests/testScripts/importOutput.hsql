import common AS comms;
// A comment
-- A comment
/*
 * A big comment.
 * A lot of words
 */

p = select * from comms.WeatherDataSet where avgg>87;
OUTPUT p;
OUTPUT p expire 3 ;
OUTPUT p title 'Weather' expire 3;
OUTPUT p file '~hsql::outputtests::Hello.csv' csv overwrite;
OUTPUT p file '~hsql::outputtests::Hello.thor' thor overwrite expire 3;
OUTPUT p file '~hsql::outputtests::Hello.json' json overwrite;
