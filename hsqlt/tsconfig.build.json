{
    "extends":"./tsconfig.json",
    // remove the tests
    "exclude": [
        "src/**/*.spec.ts",
        "src/test/*.ts"
    ],
    "compilerOptions": {
        // trim out the comments -> should help in either way for output
        "removeComments": true,
    }
}
