import %.a;
import pc;
//import %.a;
//p = select distinct *,count(c1) from a.t1 as b,a.t2 as aa group by c1 limit 5 ;

//p = select * from (select * from a.t2) as t1 order by c3;
export P = select * from a.t1 order by c1;

-- output p;
-- select * from p;
-- output p;
-- plot from p title x type bar;
-- output p title hello overwrite;
-- q = select * from a.t1 order by c1 desc;
-- output q hello overwrite;
-- 