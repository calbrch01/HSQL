// Our Current H-SQL Syntax.
table_name = layout(
  INTEGER column1, 
  INTEGER column2 
);
OUTPUT 1 TITLE SimpleAction ;

/* SQL Syntax.
CREATE TABLE table_name (
  column1 datatype,
  column2 datatype,
);

Result should be :
table_name := {
  INTEGER column1,
  INTEGER column2
};
OUTPUT(1,NAMED('SimpleAction'));
*/