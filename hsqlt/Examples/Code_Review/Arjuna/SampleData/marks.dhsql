declare subs as table( integer subid,string subname);
declare marks as table( integer subid,real marks,integer testNumber);
declare marksrec as layout (integer subid, real marks, integer testNumber);
declare subjectrec as layout (integer subid,string subname);