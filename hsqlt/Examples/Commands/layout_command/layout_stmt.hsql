export rec = create layout(
    int field1,
    int price,
    string brand,
    string model,
    int year,
    string title_status,
    real mileage,
    string color,
    string vin,
    string lot,
    string state,
    string country,
    string condition
);
export ds = select * from '~hsql::usa_cars.thor' layout rec;
-- output ds;