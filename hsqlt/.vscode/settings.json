{
    "editor.formatOnSave": true,
    "files.exclude": {
        "**/node_modules": true
    },
    // only the spec files are put here, complete tests are not here for simplicity
    "mochaExplorer.files": "src/**/*.spec.ts",
    "mochaExplorer.require": "ts-node/register"
}
